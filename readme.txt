1. run python db_init.py to initialize and populate database
    
    script will create books.db file in database/ directory, next it will
    create tables using files located in database/schemas/ directory, 
    and populate them with data stored inside database/seeders/ directory.

2. run python app.py to run application

admin username: admin
password: password

localhost:5000/  -- root page
localhost:5000/admin/  -- admin root page
localhost:5000/login/  -- login page 
